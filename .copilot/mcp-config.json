{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "${COPILOT_HOME}/../"
      ],
      "description": "Local filesystem MCP server — scoped to ~/work. Disabled by default; enable per-project via disabledMcpServers."
    },
    "fetch": {
      "command": "uvx",
      "args": [
        "mcp-server-fetch"
      ],
      "description": "Web fetch MCP server — fetch documentation and public APIs. Disabled by default; enable per-project via disabledMcpServers."
    }
  }
}
